import { schedule } from "node-cron";
import {
  checkAndUpdateClassSchedule
  // , sendReminder
} from "./class.schedule";
//
export const initializeCrons = () => {
  // schedule(`* * * * *`, () => {
  //   checkAndUpdateClassSchedule();
  //   // sendReminder(15);
  //   // sendReminder(24);
  // });
};
